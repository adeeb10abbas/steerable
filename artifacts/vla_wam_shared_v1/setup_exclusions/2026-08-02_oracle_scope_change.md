# Predicate-oracle scope-change exclusion

At 2026-08-02T20:28:34Z, the running `v1-cosmos-h5-oracle` container was
stopped because the study question changed from privileged hierarchical
controllability to direct task-language steerability. The thermal guard ended
normally without an emergency event.

The batch had written eight complete episode records: all five left episodes
and right runs 0–2. All eight recorded `success: false`. Right run 3 had begun
but had no completed episode record and is not a valid attempt. These outcomes
were visible before `direct_language_scope_amendment_003.json` was frozen and
are disclosed there.

The raw directory was moved to:

```text
/home/ali/projects/RoboLab/output/v1_cosmos_h5_oracle_scope_change_excluded
```

The guard log was moved to:

```text
artifacts/vla_wam_shared_v1/setup_exclusions/cosmos_h5_oracle_scope_change_thermal.jsonl
```

Neither the eight completed records nor the interrupted ninth run may enter a
direct-language denominator, model comparison, stopping decision, prompt
selection, or headline claim. They are retained only to make the scope change
and observed evidence auditable.
