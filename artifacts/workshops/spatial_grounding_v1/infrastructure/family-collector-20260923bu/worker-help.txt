usage: family_partition_summary.py [-h] --config CONFIG --workers-root
                                   WORKERS_ROOT --output OUTPUT

Read-only compact accounting for a frozen family partition collection.

options:
  -h, --help            show this help message and exit
  --config CONFIG
  --workers-root WORKERS_ROOT
  --output OUTPUT
