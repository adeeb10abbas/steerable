apiVersion: batch/v1
kind: Job
metadata:
  name: sgw01-ali-lat-measurement-contract-audit-20260922j
  namespace: 211247-prod
  labels:
    owner: ali
    app.kubernetes.io/name: sgw-01
    app.kubernetes.io/managed-by: ali-research
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 0
  activeDeadlineSeconds: 180
  template:
    metadata:
      labels:
        owner: ali
        app.kubernetes.io/name: sgw-01
        purpose: read-only-lat-measurement-contract-audit
    spec:
      restartPolicy: Never
      terminationGracePeriodSeconds: 30
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: artifactory-ci-pull-secret
      securityContext:
        fsGroup: 2518800
        supplementalGroups: [2518800]
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: audit
          image: artifactory-ci.gm.com/docker-approved/devcontainers/base@sha256:03f5ce7d090fbd378070a8216d0aedfc6e473c52da99b40b0cf53918612a297c
          command: ["/bin/bash", "-ec"]
          args:
            - |
              set -euo pipefail
              ROBOLAB=/data/users/ali/vla_wam/external/RoboLab-pi05-v3-0aef241
              STUDY=/data/users/ali/sgw-01/source/b429ddd
              ISAACLAB=/data/users/ali/vla_wam/envs/robolab-v2-isaac50/lib/python3.11/site-packages/isaaclab
              RIGID="$ISAACLAB/assets/rigid_object/rigid_object_data.py"
              WORLD="$ROBOLAB/robolab/core/world/world_state.py"
              test "$(git -C "$ROBOLAB" rev-parse HEAD)" = 0aef241fb088ca21bb4ebd24448940ed56620d17
              test "$(git -C "$STUDY" rev-parse HEAD)" = b429ddd8c3e7be59f63759c1820e352029e48b8c
              test -f "$RIGID"
              test -f "$WORLD"
              printf '%s\n' '=== pinned source hashes ==='
              sha256sum "$RIGID" "$WORLD"
              printf '%s\n' '=== IsaacLab RigidObjectData velocity properties ==='
              sed -n '/def root_vel_w(/,/^    @property/p;/def root_lin_vel_w(/,/^    @property/p;/def root_com_vel_w(/,/^    @property/p;/def root_link_vel_w(/,/^    @property/p' "$RIGID"
              printf '%s\n' '=== RoboLab geometry cache and centers ==='
              sed -n '/def _get_local_geometry(/,/^    def /p;/def get_bbox(/,/^    def /p;/def get_local_geometric_center(/,/^    def /p' "$WORLD"
          resources:
            requests:
              cpu: "1"
              memory: 1Gi
            limits:
              cpu: "1"
              memory: 1Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            runAsGroup: 2518800
            runAsNonRoot: true
            runAsUser: 816149040
          volumeMounts:
            - name: workspace
              mountPath: /data
              readOnly: true
            - name: tmp
              mountPath: /tmp
            - name: home
              mountPath: /home/ali
            - name: userdb
              mountPath: /etc/passwd
              subPath: passwd
              readOnly: true
            - name: userdb
              mountPath: /etc/group
              subPath: group
              readOnly: true
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: 211247-prod-pvc
            readOnly: true
        - name: tmp
          emptyDir: {}
        - name: home
          emptyDir: {}
        - name: userdb
          configMap:
            name: 211247-ali-b200-1gpu-userdb
