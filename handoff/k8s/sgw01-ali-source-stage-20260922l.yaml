apiVersion: batch/v1
kind: Job
metadata:
  name: sgw01-ali-source-stage-20260922l
  namespace: 211247-prod
  labels:
    owner: ali
    app.kubernetes.io/name: sgw-01
    app.kubernetes.io/managed-by: ali-research
spec:
  completions: 1
  parallelism: 1
  backoffLimit: 0
  activeDeadlineSeconds: 300
  template:
    metadata:
      labels:
        owner: ali
        app.kubernetes.io/name: sgw-01
        purpose: source-stage
    spec:
      restartPolicy: Never
      terminationGracePeriodSeconds: 30
      automountServiceAccountToken: false
      imagePullSecrets:
        - name: artifactory-ci-pull-secret
      securityContext:
        fsGroup: 2518800
        supplementalGroups: [2518800]
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: stage
          image: artifactory-ci.gm.com/docker-approved/devcontainers/base@sha256:03f5ce7d090fbd378070a8216d0aedfc6e473c52da99b40b0cf53918612a297c
          command: ["/bin/bash", "-ec"]
          args:
            - |
              set -euo pipefail
              BASE=/data/users/ali/sgw-01/source/b429ddd
              OUT=/data/users/ali/sgw-01/source/35e627e
              COMMIT=35e627eda09f24f00995fc2dce4dbee362c0bc02
              test -d "$BASE/.git"
              test ! -e "$OUT"
              git clone --no-checkout "$BASE" "$OUT"
              git -C "$OUT" fetch --depth=1 origin "$COMMIT"
              git -C "$OUT" checkout --detach "$COMMIT"
              test "$(git -C "$OUT" rev-parse HEAD)" = "$COMMIT"
              test -z "$(git -C "$OUT" status --porcelain)"
              git -C "$OUT" fsck --no-dangling
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "1"
              memory: 2Gi
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
            runAsGroup: 2518800
            runAsNonRoot: true
            runAsUser: 816149040
          volumeMounts:
            - name: workspace
              mountPath: /data
            - name: tmp
              mountPath: /tmp
            - name: home
              mountPath: /home/ali
            - name: userdb
              mountPath: /etc/passwd
              subPath: passwd
              readOnly: true
            - name: userdb
              mountPath: /etc/group
              subPath: group
              readOnly: true
      volumes:
        - name: workspace
          persistentVolumeClaim:
            claimName: 211247-prod-pvc
        - name: tmp
          emptyDir: {}
        - name: home
          emptyDir: {}
        - name: userdb
          configMap:
            name: 211247-ali-b200-1gpu-userdb
