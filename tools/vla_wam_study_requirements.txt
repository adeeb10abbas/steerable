# CPU-side evidence compilation and plotting only.
#
# Install into the project environment with:
#   uv pip install --python .venv/bin/python -r tools/vla_wam_study_requirements.txt
#
# Cosmos inference and Qwen semantic scoring use the separately pinned
# /home/ali/cosmos-framework/.venv environment and are intentionally excluded.
h5py==3.16.0
matplotlib==3.11.1
numpy==2.5.1
opencv-python-headless==5.0.0.93
pandas==3.0.5
scipy==1.18.0
