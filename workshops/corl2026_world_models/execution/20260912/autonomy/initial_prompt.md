# Execute the GM forecast ablation autonomously

The user explicitly authorized the entire attached core ablation on the GM cluster, aggressive parallelism, detached execution that survives SSH disconnects, autonomous continuation, and pushing the work and results. Their latest instruction is: “its awake now launch it all so that it keeps running even if the ssh connection gets disconnected. ask it so it runs everything and pushes everything from the cluster autonomously!”

You are the durable coordinator running on the workstation. This is an execution assignment, not a request for another plan. Implement the missing runtime pieces, qualify the recording, execute the eligible study stages, analyze the evidence, and push progress/results. Do not stop after software tests or present a plan as completed experiments. Continue meaningful work between turns; the supervisor will resume this exact session. Do not ask again for authorization already provided here. If a prerequisite is missing, resolve it within the specified design rather than treating its existence as a request for new permission.

## Latest operational requirement: remove the work Mac before departure

The user clarified that the workstation will always remain active, but the work Mac goes offline at **7 a.m. Eastern on 13 September 2026**. Treat 2026-09-13T11:00:00Z as the conservative cutoff. Your Codex supervisor stays on the workstation; all cluster dispatch and result return must work without the Mac/VPN hop by then. Do not rely on cluster-local Codex: its approved current network cannot reach the Codex service, and its default serviceaccount cannot manage Kubernetes Jobs. Do not copy GM kubeconfig, workstation Codex login state or broad GitHub tokens into the cluster.

The parent is provisioning a normal Git-based deployment/result channel. A cluster-local coordinator polls this repository's `codex/forecast-layout-gm-20260912` control branch over GitHub, stages immutable source commits and released queue descriptors on the PVC, and fixed pre-created GPU workers claim jobs from that shared queue. Cluster results are pushed independently to `codex/forecast-layout-gm-20260912-results`; fetch that branch from the workstation for exact receipts, stdout tails and deliberately published small artifacts/images. No cluster OpenAI access or later Kubernetes API access is needed for this channel. Existing private source/runtime/checkpoints stay on the cluster.

Read `autonomy/cluster_queue.py` and `autonomy/cluster_queue.json` when present for the exact schema and commands. The parent will write deployment receipts and `/home/ali/wmf-forecast-autonomy-20260912/operator_updates.md`; inspect that file at every milestone and before dispatch/publishing. While the parent bootstrap is still in progress, work on the scientific recorder, source checks and analysis in separate workshop files; do not alter bootstrap infrastructure or start a duplicate coordinator. Use direct GM SSH only while available for the initial setup. As soon as the queue independence receipt is present, use queue commits/results-branch receipts for ongoing GPU work and demonstrate this route with real qualification jobs before departure. The static worker pool can execute all 58 planned blocks sequentially/parallel as released; do not equate worker count with scientific sample size.

The cluster's repo-scoped deploy key has GitHub ID163118482. Its private half remains under `/data/users/ali/vla_wam/private/wmf_ablation_001_20260912/` and must never be copied, logged or committed. Once all cluster results are safely pushed and no cluster worker/controller needs that key, the workstation may revoke precisely this task-owned deploy key using its normal GitHub access. Do not delete other keys or branches.

## Authoritative workspace and initial reading

Work only on `/home/ali/projects/steerable-forecast-layout-20260912`, branch `codex/forecast-layout-gm-20260912`. The scientific authority is `workshops/corl2026_world_models/docs/ABLATION_SPEC.md`, supported by its machine-readable spec and planned cell CSV under `experiments/forecast_layout`. Read `AGENTS.md`, the spec, and `workshops/corl2026_world_models/execution/20260912/{CONTINUE.md,status.json,runtime_readiness.md,parallel_schedule.json,nano_seed_audit.json,archive_recovery_targets.json}`. Historical preparation status saying the network is blocked is superseded by the verified reconnection below; inspect live state yourself. Historical V2/V3 registries and all unrelated work stay untouched.

Source commits `e67e6c4f990030fa8dc9b40a7e5dc713c99457e5` (spec) and `ce561e66f82e95055e39d3d7711691982f6b2086` (historical evidence/runtime) are present in this repository. Use `git show` for sparse files and extend only this checkout's sparse patterns when needed. The preparation has 43 passing workshop tests, a reviewed 58-job/232-cell schedule, and reproduced historical stopping controls. Those are not new model runs. All new work belongs under `workshops/corl2026_world_models/`; large raw recordings and weights belong on the existing GM PVC.

## Verified access, autonomy, and publishing authority

This workstation can directly reach the work Mac using `ssh -4 -o BatchMode=yes -o ConnectTimeout=10 -o ServerAliveInterval=30 -o ServerAliveCountMax=3 SZ5VJY@ZTMACMP49KLV4LX.local`. The work Mac's Kubernetes binary is `/usr/local/bin/kubectl`. Use context `prod-dcwi-warrenq1-vmkub007` and namespace `211247-prod`. Keep GM credentials on the work Mac; do not copy kubeconfig/auth files. Do not use the workstation's unrelated default Kubernetes context.

At reconnection, owned pod `211247-ali-b200-1gpu` was Running; its one B200 had 183359 MiB total, 182632 MiB free, zero utilization and no compute processes. Its existing persistent mount is `/data`, PVC `211247-prod-pvc`; `/data/users/ali/vla_wam` is the user's writable root. These are observations to refresh before launch, not permanent allocation guarantees. Read `cluster_live_inventory.json` if present. Historical multi-GPU capacity is not current capacity. Inspect only ali-owned resources, confirm no conflicting processes, and use existing idle capacity or create appropriately labelled owned worker pods using the approved existing image/PVC and verified scheduling constraints. Do not interfere with other users or unrelated jobs. Provision only the bounded workers required for this study, and clean up owned temporary resources after their outputs are safely retained.

Use the supplied block schedule: at most 2 simultaneous pilot jobs, 8 development jobs, and 48 confirmation jobs; each contains one model and an intact four-condition layout block. Choose actual concurrency from measured memory/runtime and verified capacity. Use separate simulator/policy GPUs where the existing qualified integration requires them. Fully isolate each model context and verify resets. Never interleave episodes on a global-state DreamZero server. Model loading should be amortized within isolated workers when its reset contract permits it. Parallelize independent code, source recovery, asset checks, physical fixture checks, inference blocks, decoding and analysis with available subagents/processes; prevent overlapping ownership or duplicate dispatch.

Launch actual cluster controllers in detached process groups or Jobs with source/log/output paths on the PVC. A dropped `kubectl exec` or SSH session must not kill a running episode. Maintain persistent ledgers, unique attempt directories, locks/leases, process identity and exit receipts. Before resuming, reconcile live processes and output receipts; never rerun a valid completed cell. Preserve incomplete and technically invalid attempts separately. Retry transient connectivity with backoff while jobs continue. The personal laptop is not part of the execution chain.

The authorized publishing destination is `https://github.com/adeeb10abbas/steerable.git`, dedicated branch `codex/forecast-layout-gm-20260912`. This checkout has remote `publish` for that URL; `origin` is only the imported source bundle. Push the dedicated branch after each coherent implementation/evidence slice and at final completion, using normal fast-forward pushes. Never force-push or merge into the default branch. Review staged files and exclude credentials, checkpoints, environments, and bulk raw arrays/videos from ordinary Git. Push complete compact manifests, hashes, counts, analysis, reports and appropriate representative media. Preserve all raw evidence on `/data/users/ali/vla_wam/raw/wmf_ablation_001_20260912` and record exact recoverable paths/checksums in pushed manifests. If the repository already provides a suitable artifact/LFS archive, use that verified mechanism for large results. The workstation currently has only about 17 GB free, so do not mirror the entire raw corpus there.

## Scientific execution requirements

The selected core is up to 8 recording pilot episodes, 32 development episodes and exactly 192 confirmation episodes if both models qualify, totaling at most 232 behavioral cells. The six nonbehavioral generation probes per primary model are separately counted. Optional D2 and unrelated expansions remain unselected. Use N3 and official conditional D1 with the exact checkpoints/configuration stated in the spec. Do not substitute historical custom s2 for D1, and do not describe fixed DreamZero noise labels as independent noise draws.

Start now with live capacity/source/archive inspection and implement the missing fixed-duration recorder while independent recoveries run. The old RoboLab configuration internally freezes environments on success: remove success from termination before constructing a new workshop task; logging success while ignoring a returned done flag is insufficient. Preserve first success as an event and execute 450 actual actions unless a separately recorded safety abort or technical failure truncates the episode. Retain original camera observations, preceding observations for constant velocity, exact model inputs/preprocessing, actions returned and executed, latent/decoded futures, timestamped request/control/physics/camera identities, settled reset states and full context-reset receipts. Do not provide simulator-state information to the policy beyond its declared interface.

Recover and hash-check one H01 and one H02 archive episode, or record a precise unavailable result based on searched host/path. Restore exact external source/weights in isolated locations; existing external repos may contain unrelated changes and must not be cleaned. Use the official DreamZero path described in runtime_readiness.md; only a demonstrated action-and-latent equivalent patched s1 may occupy D1. Run the fixed decode/repeat probes, qualify the real physical forecast/action/camera mapping and recording pilots, then run development. Never infer physical time by equating video frame number with action number or from a concatenated presentation video.

Generate distinct development/confirmation layouts by model-blind physical checks, retaining rejected candidates. Freeze the pose manifest, actual horizons/camera tolerance, sampling algorithm, annotation rubric/movement threshold, seed semantics, order and measured resource budget before confirmation. Keep all 24 confirmation base-layout pairs; do not shrink the study or choose an outcome-dependent extension for speed. If only one model qualifies, follow the spec's explicitly labelled reduced-study branch without substituting another model. Interpret any failed qualification honestly and resolve objective implementation defects without hiding earlier attempts.

Two independent image raters and the stated development validation are required. Do not silently substitute unvalidated VLM labels for ground truth or fabricate a movement-resolution threshold. Build and package the blind annotation workflow and use a legitimate existing authorized annotation route if available. If a required human labeling/input dependency cannot be completed autonomously, finish all independent authorized work, preserve running jobs, and report the exact pending input honestly. Do not release dependent confirmation prematurely or claim forecast accuracy without its labels. An unsupported timing interface or failed model is evidence about qualification, not proof of prediction skill.

After the planned cohort is complete (or a properly disclosed interruption/qualified reduced branch), produce per-model skill/baseline errors, complete coverage and missingness bounds, paired layout uncertainty, reflection contrasts, stopping controls, movement decomposition, model/sample-size table and declared-rule example videos. Preserve failures and report either sign of the results. Update the paper from actual evidence only. Do not submit the paper or communicate with third parties.

## Persistent progress and supervisor contract

Supervisor state is outside Git at `/home/ali/wmf-forecast-autonomy-20260912`. Do not start a duplicate coordinator or edit its running supervisor/service. Update the workshop execution `status.json` and `progress.md` after real milestones with exact episode/request counts, active owned jobs, source identities, remaining blockers, output locations and the next concrete action. Update the initial CONTINUE.md as execution advances; its original zero-run snapshot is historical once actual inference begins. Push compact updates promptly. Treat test counts, pilot counts, development counts and confirmation outcomes separately.

When a turn ends with work remaining, leave a precise continuation and return normally; the supervisor resumes your persisted session. Rate limits/network interruptions are transient: preserve progress and let the supervisor retry. Do not create a terminal marker merely because work is slow or one turn cannot finish.

Only when the authorized study and deliverables are actually finished, outcomes/release limitations are honestly reconciled, pushes are verified, and no owned compute needs monitoring, write `/home/ali/wmf-forecast-autonomy-20260912/completion.json` with `status: "complete"`, `safe_to_stop: true`, a nonempty `summary`, and a nonempty `evidence` list of existing result/provenance/remote-push receipts. Include final requested/planned/launched/completed/invalid/censored/unrun counts per stage/model and the push commit. Do not claim all 232 ran if a branch was unqualified.

For an unavoidable dependency on user input or a security boundary after all independent work is done, write `/home/ali/wmf-forecast-autonomy-20260912/needs_input.json` with `status: "needs_input"`, `safe_to_stop: true`, a specific nonempty `reason`, required action, current counts, and safe live-job status. Set `safe_to_stop` only after live jobs have finished or their continued monitoring and retry/recovery are owned by verified independent durable controllers; a user-input dependency must never strand live compute. This is not appropriate for routine fixes, initial missing implementations, ordinary retries, or repeated permission requests. Do not mark completion in that case. Both markers must be written atomically and must never hide active jobs.

Begin with a real live inspection, then start the necessary implementation and permitted qualification work. The intended outcome is actual completed research with recoverable results, not another offline preparation handoff.
